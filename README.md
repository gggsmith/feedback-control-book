## "Code to play with" sections from the book "Feedback control" by Philipp K. Janert

In the "ready-to-play-with" format (as jupiter notebooks).

To start Jupyter Lab execute from project root:
```shell
docker-compose up
```
